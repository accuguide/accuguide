procs:
  docker:
    cmd: ["docker", "compose", "up"]
    stop:
      send-keys: ["<C-c>"]
  next.js:
    cmd: ["bunx", "next", "dev"]
    stop:
      send-keys: ["<C-c>"]
  shell:
    shell: bash
    stop:
      send-keys: ["<C-d>"]
