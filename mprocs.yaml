procs:
  docker:
    cmd: ["docker", "compose", "up"]
    stop:
      send-keys: ["<C-c>"]
  next.js:
    cmd: ["bun", "dev:cy"]
    stop:
      send-keys: ["<C-c>"]
  shell:
    shell: zsh
    stop:
      send-keys: ["<C-d>"]
